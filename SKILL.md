---
name: agent-prd
description: 为 AI Agent 产品撰写通用 PRD（产品需求文档）的结构化工作流。当用户需要"设计一款 agent"、"写 agent 产品 PRD"、"AI 产品需求文档"、"agent 产品规划/立项"、"设计 AI 助手/智能体产品"时触发。覆盖痛点定位、用户场景、Agent 架构（模型/工具/Prompt 封装/RAG/记忆）、人机协同、权限安全、评估指标、商业化与里程碑八大模块，输出可直接评审的完整 PRD。
---

# Agent PRD 设计工作流

为 AI Agent 产品产出一份可评审、可落地的 PRD。全程使用祈使句与决策树推进；每个阶段先收集输入，再产出对应 PRD 章节。

## 核心原则

- 先问清约束再动笔：Phase 0 的问题未回答前，不进入架构设计。
- 每个架构决策必须写清"理由 + 被否决的备选"，PRD 不是功能清单，是决策记录。
- 指标先于功能：任何核心功能必须挂载可度量的评估指标（见 references/evaluation-metrics.md）。
- 输出物默认写入 `agent-prd-<产品名>-<日期>.md`，结构严格遵循 references/prd-template.md。

## 工作流

### Phase 0 · 需求澄清（门禁阶段）

向用户确认以下 5 个问题（允许用户说"你来假设"，则将假设显式写入 PRD 的"前提与假设"节）：

1. 目标用户是谁？（开发者 / 知识工作者 / 消费者 / 企业内部）
2. Agent 要替代或增强什么现有工作流？
3. 运行形态：chat 助手 / IDE 内嵌 / CLI / 后台自动化 / 浏览器？
4. 数据边界：处理的是公开数据、用户私有数据还是企业多租户数据？
5. 模型策略：自研模型 / 单一第三方 / 多模型路由？

### Phase 1 · 痛点与产品定位

- 用"旧世界 → 痛点 → 新解法"三列表格陈述，禁止泛泛的"提升效率"。
- 写清一句话定位：产品 = 什么形态 × 什么能力 × 为谁解决什么。
- 明确不做什么（Non-goals），至少 3 条。

### Phase 2 · 用户与核心场景

- 定义 1–2 个用户画像（角色、技术熟练度、现有工具链）。
- 写 3–5 个核心场景，每个场景按"触发 → 用户输入 → Agent 行为序列 → 成功产出 → 失败兜底"展开。
- 标注每个场景的频率（日/周/月）与价值（省多少时间/降多少错）。

### Phase 3 · Agent 架构设计（PRD 核心章）

按以下 6 个子模块逐一决策，每个决策查阅 references/agent-design-patterns.md 对应章节：

1. **模型层**：选型与路由策略（旗舰/轻量分工、Auto 路由、自研 vs 第三方）。
2. **工具集**：列出工具清单（名称、能力、权限级别、失败处理）；优先原语工具（shell/文件/搜索）还是垂直集成，须给理由。
3. **Prompt 封装**：系统 Prompt 的分层结构（身份/规则/工具/格式/护栏）；静态 vs 动态分区（缓存成本）；用户不可见的行为规范。
4. **上下文与 RAG**：用决策树选型——预建索引（嵌入+向量库）/ Agentic 检索（工具自主探索）/ 混合；写清切块、排序、权限过滤、刷新策略。
5. **记忆**：会话内（计划外化、压缩）、跨会话（持久记忆文件）、用户可控记忆的分层设计。
6. **Agent 循环**：循环控制（最大轮次、止损规则）、子 Agent 隔离策略、人机断点（何时必须问人）。

### Phase 4 · 人机协同与安全权限

- 权限模型：逐动作审批 / 只读规划模式 / 全自动，各自适用场景。
- 危险操作清单与拦截策略（删除、外发、支付、覆盖写）。
- 数据合规：训练数据使用、保留策略、企业租户隔离、注入攻击防护。
- 失败兜底：拒答策略、降级路径、人工接管入口。

### Phase 5 · 评估与指标

- 按 references/evaluation-metrics.md 为每个核心场景挂载指标：任务成功率、检索质量、引用/事实正确率、延迟、成本、安全事件。
- 定义评测集规模（建议 200–500 条覆盖各场景与难度）与回归门槛（每次发版必跑）。

### Phase 6 · 商业化

- 定价结构选型：订阅 / 按量 / 双轨 / 捆绑升舱，写清单位经济学（单用户日均 token 成本 vs 定价）。
- 免费层定位：获客漏斗的哪一环，限额如何设计。
- 护城河陈述：数据飞轮 / 基础设施 / 生态标准 / 切换成本，至少论证一条。

### Phase 7 · 里程碑与风险

- 三段式路线图：MVP（验证核心闭环）→ V1（补齐检索质量与评估体系）→ 规模化（成本与护城河）。
- 风险表：技术风险（幻觉、上下文爆炸）、商业风险（算力成本、模型依赖）、合规风险，各配缓解措施。

## 产出与质检

1. 按 references/prd-template.md 的章节结构产出完整 PRD。
2. 交付前逐项过质检清单（模板末尾），确认：每个功能有指标、每个架构决策有理由、每个风险有缓解、数字自洽。
3. 向用户汇报时，先给一句话定位与三个最关键的架构决策，再给文档路径。

## 参考文件

- [references/prd-template.md](references/prd-template.md) — PRD 完整章节模板（填空式，含质检清单）。产出 PRD 前必读。
- [references/agent-design-patterns.md](references/agent-design-patterns.md) — Agent 设计模式速查（模型路由、Prompt 分层、RAG 决策树、权限模型、定价模式）。Phase 3/4/6 做架构决策时查阅。
- [references/evaluation-metrics.md](references/evaluation-metrics.md) — 评估指标体系与评测集设计方法。Phase 5 必读。
